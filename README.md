# Vigilant version

#### Create wheel
``` python setup.py bdist_wheel```

#### Install using pip
```pip install .```

#### Bump Version
```bumpversion --config-file .bumpversion.cfg patch --verbose```